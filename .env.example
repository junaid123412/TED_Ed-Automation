# HTTP Basic Auth credentials (required before the app loads)
HTTP_AUTH_USERNAME=your_basic_auth_username
HTTP_AUTH_PASSWORD=your_basic_auth_password

# TED-Ed account credentials (used for logging in)
TED_LOGIN_EMAIL=your_ted_login_email
TED_LOGIN_PASSWORD=your_ted_login_password
